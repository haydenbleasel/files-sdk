---
"files-sdk": minor
---

Add `files-sdk/vue` — the Vue 3 twin of the React hook: a `useFiles` composable returning refs for the ambient state, plus reactive `useList`/`useFile`/`useSearch` composables over the same gateway.
