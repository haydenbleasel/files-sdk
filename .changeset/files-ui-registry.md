---
"files-sdk": minor
---

Add a shadcn component registry of `useFiles`-wired UI: `dropzone`, `file-list`, `file-preview`, `upload-progress` and `multipart-uploader`, installable with `npx shadcn add`. The components ship in the docs site rather than the package, but they're a first-class part of the SDK surface.
